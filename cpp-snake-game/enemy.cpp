#include <cstdlib>
#include "enemy.h"

// TODO: unit tests in C++
enemy::enemy(pair field_size, pair size) {
    this->field_size = field_size;
    this->size = size;
    this->position = pair(0, 0);
//    int max_possible_x = field_size.first;
//    int y = rand() %;
}
